<?xml version="1.0" encoding="utf-8"?>
<!--
  Este es el layout correcto para la lista.
  Usa CoordinatorLayout para que el FloatingActionButton (FAB)
  pueda mostrarse correctamente sobre el RecyclerView.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.PatientListFragment">

    <!-- El RecyclerView para mostrar la lista de pacientes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_patients"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:listitem="@layout/item_patient" />

    <!-- El Botón Flotante para añadir nuevos pacientes -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_patient"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/app_color"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="50dp"
        android:contentDescription="@string/a_adir_nuevo_paciente"
        android:src="@android:drawable/ic_input_add"
        app:tint="@android:color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
